# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Purpose

This is a personal notes and learning repository for exploring Claude Code (openclaw) and git. It contains no build system, tests, or application code.

## Key Files

- `openclaw 명령어` — Claude Code quick reference in Korean: keyboard shortcuts (Shift+Tab for Plan/Accept mode, Escape to stop, `!` prefix to run bash) and slash commands (/init, /clear, /compact, /context, /models, /resume, /mcp, /export)
- `진짜테스트` — git usage test file
